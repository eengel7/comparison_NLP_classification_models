
name: "preprocessing"

paths:
  # Loading raw data
  raw_data: data/raw_data/Finansierade projekt.xlsx # Funded projects
  classifications: data/raw_data/SCB_classifications.xlsx # SCB based classifications

  preprocessed_path: data/preprocessed_for_${model.name}
  # # Location of preprocessed data 
  # train_set_path: data/preprocessed_${embedding}
  # test_set_path: data/preprocessed_${embedding}

# # column names
# # SCB_labels: Label_En
# # description: Description En
# # label: Research fields
# # title: Title En

tokenize:
  remove_stopwords: True
  apply_stemming: True

# SCB classifications 
digits: 3   # Alternatives: 3,5        Number of digits indicating the level of classification

test_size: 0.20 


